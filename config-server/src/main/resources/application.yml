server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/barahona1998MP/msvc-prueba
          search-paths: config-data


